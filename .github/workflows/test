name: Push test
run-name: ${{ github.actor }} is testing out the push test
on: pull_request
jobs:
  build-db-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: echo test
        run: echo "test"
